Generation output has to sound like Nathan. Phase 1 ships "correct" — this ticket makes it "Nathan's voice."

**Scope:**

1. **Collect real samples.** Resume bullets Nathan actually uses (approved after generation, or manually written). Cover letter paragraphs from previous applications.
2. **Identify voice signatures.** What Nathan does that the default prompt doesn't: short declarative sentences, action verbs at the start, specific metrics, resists puffery, drops the phrase "cross-functional" on principle.
3. **Iterate the prompt.** Add a voice section to the generation prompts (resume + cover letter). Include 3–5 approved bullets as few-shot "do write like this"; include 3–5 generic-sounding bullets as "do not write like this."
4. **Preserve zero-fabrication.** Voice tuning never comes at the cost of validation. Tighter prompts mean fewer claims, not more invented ones.
5. **Specificity.** Generic output ("collaborated cross-functionally to drive alignment") is a validation-layer concern too (Phase 1 #c8). If the generation prompt still produces vague phrasing that the validation layer has to flag, the prompt is wrong and this ticket fixes it — the validation layer is the safety net, not the style editor.

**Exit criteria:**
- Nathan sight-reads three generated resume outputs and three cover-letter outputs and signs off that they sound like him.
- Zero specificity flags on the final outputs (if validation still flags, the prompt needs more work).

Parent: #__PARENT_NUM__
