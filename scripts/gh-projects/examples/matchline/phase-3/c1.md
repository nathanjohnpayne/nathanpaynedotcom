Improve the extraction prompt against Nathan's real resumes.

**Scope:**

1. **Collect real data.** Nathan's current resume plus prior versions. At least 3 distinct versions covering different periods / angles of his career.
2. **Run extraction on each.** Record the output.
3. **Label errors.** For each extraction, manually mark: missed Units, incorrect Units, wrong `confidence_score`, wrong `unit_type`, wrong `metrics`, wrong `date_range`.
4. **Analyze patterns.** Common error modes get prompt fixes; rare ones get acknowledged and logged for Phase 4 (if there is one).
5. **Iterate the prompt.** Small, targeted edits. Re-run the Phase 1 evaluation harness after each iteration to confirm the fixture-set accuracy doesn't regress.
6. **Add regression fixtures.** Nathan's resumes (with sensitive fields redacted if needed) become fixtures in the eval harness. They stay in forever.

**Exit criteria:**
- Fixture-set extraction accuracy ≥ 90% on Nathan's resumes (stricter than the general 80% bar — this is his data).
- No regression in the general 10-resume fixture set.

Parent: #__PARENT_NUM__
