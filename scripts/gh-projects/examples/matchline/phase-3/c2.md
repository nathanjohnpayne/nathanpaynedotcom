Feed Nathan's approve/reject patterns back into the matching engine.

**Scope:**

1. **Capture approve/reject signal.** Every `UnitMatch.approved_for_use` / `user_rejected` toggle is already persisted. Query for a table of (Match → user decision), grouped by the component breakdown that produced the `final_score`.
2. **Find patterns.** Offline analysis: are matches with high `skill_overlap` but low `domain_overlap` disproportionately rejected? Are high-`recency` matches from a less-relevant role approved more than they should be? Pivot by `unit_type` and `category`.
3. **Iterate on weights.** Adjust the weights in the scoring formula based on patterns. Keep the structure from spec § Matching engine; only the coefficients are data-driven. Any weight change is PR'd with: (a) the pattern that motivated it, (b) the expected behavior change, (c) before/after accuracy on the fixture set.
4. **Rationale tuning.** If Nathan's rejections cluster around weak rationale strings, tune the rationale prompt — make it lead with the dominant component, surface the specific overlap evidence, be more cautious when confidence is low.
5. **Regression.** All weight changes go through the Phase 1 eval harness and may not reduce fixture-set match accuracy below 80%.

**Non-goals:**
- Machine-learned weight tuning — V2 feature, needs more data volume.
- Per-user ontology refinement — V2.

**Exit criteria:**
- Real-data match accuracy ≥ 85% on manual review of Nathan's active applications.
- No regression in fixture-set accuracy.

Parent: #__PARENT_NUM__
