The PRD's V1 primary success metric: **Nathan ships ten serious applications through Matchline.**

> The primary metric. Does the user (me) use it as the primary job search tool for the entire active search period, or does it get abandoned for a spreadsheet and ChatGPT? If it's used end-to-end for at least ten serious applications, V1 is validated.

**Scope:**

- Every application shipped through Matchline during the active search counts toward the 10.
- "Shipped" means: resume + optional cover letter generated through Matchline, validated, exported, actually submitted to the employer, and the Application is marked `Applied` in the Pipeline.
- Serious means: Nathan would want the job, not a throwaway.

**Validation of the milestone:**

- Pipeline view shows 10 applications in `Applied` (or further along) with `generated_assets` populated.
- Cost dashboard shows real per-app spend within the $1 budget at p95.
- Zero fabrication: walk any three random final resumes; every claim traces.
- Nathan confirms Matchline was the primary tool — no parallel spreadsheet, no ChatGPT detour for "the hard ones."

**If the milestone doesn't close:**
- If Nathan finds the tool lacking and falls back to old habits, that's the signal the PRD anticipates. The PRD is explicit about this: "a version of this tool that generates beautiful applications that never convert is a failed tool." Document why, and that becomes the V2 starting point.

Parent: #__PARENT_NUM__
