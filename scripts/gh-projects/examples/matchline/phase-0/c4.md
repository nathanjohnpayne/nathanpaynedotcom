Merge PR #4 (Sprint 0 scaffold) into `main` once review and CI are clean.

**Blocks on:**
- #__C1_NUM__ (P0 Haiku model ID fix)
- #__C2_NUM__ (P1 owner-scoping seam)
- #__C3_NUM__ (CI workflow fixes)
- `nathanpayne-claude` can post a review (requires human-action sub-issue for collaborator access).

**Scope:**
- Confirm all CI workflows pass.
- Confirm `nathanpayne-claude` has approved the PR with no outstanding P0/P1 findings.
- If PR size crosses `external_review_threshold` in `.github/review-policy.yml` (300 lines), follow the Phase 4 external-review flow in `REVIEW_POLICY.md`. PR #4 is just over the threshold by raw count but most of that is lockfiles; confirm the check's line-counting rule before assuming external review is needed.
- Merge as `nathanjohnpayne` with `gh pr merge --squash --delete-branch`. Do not use `--admin`.

Parent: #__PARENT_NUM__
