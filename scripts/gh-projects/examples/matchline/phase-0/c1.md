**P0 review finding on PR #4.**

`functions/src/llm/config.ts` assigns `claude-haiku-4-5` as the model ID for the `requirement_parsing` and `generation` stages. That is not a valid Anthropic API model ID — Haiku 4.5's canonical ID is `claude-haiku-4-5-20251001`. Calls to those stages will 404 on first use.

**Scope:**
- Edit `functions/src/llm/config.ts` → replace both `claude-haiku-4-5` entries with `claude-haiku-4-5-20251001`.
- Leave `claude-sonnet-4-6` entries unchanged (correct as-is).
- No behavior change besides the model ID.

**Verification:**
- `npm --prefix functions run build` passes.
- `grep -nR "claude-haiku-4-5\"" functions/` returns no matches (only the dated form).

Parent: #__PARENT_NUM__
