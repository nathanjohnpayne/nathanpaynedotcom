**Three CI workflows fail on every PR in this repo.** All three are template-inherited and need per-repo setup.

**Failures observed on PR #4:**

1. **External Review Check** — `gh pr edit --add-label needs-external-review` fails because the label doesn't exist in the repo yet.
2. **triage** — `Error: Input required and not supplied: github-token`.
3. **assign** — same `github-token` input missing.

**Scope:**
- Create the `needs-external-review` label in the repo (ideally via the `create-issues.sh` driver's `ensure_label` call — already included).
- Read the `triage` and `assign` workflow YAML. Either:
  - Update the workflow to use `${{ secrets.GITHUB_TOKEN }}` where sufficient, or
  - Document the required repo secret (e.g. `AGENT_PAT`) and add a `[Human]` sub-task for storing it. Check how `nathanpaynedotcom` solves this — the same workflows exist there and pass.

**Verification:**
- Open a throwaway PR, confirm all three workflows pass (or are appropriately gated).
- Close the throwaway PR.

Parent: #__PARENT_NUM__
