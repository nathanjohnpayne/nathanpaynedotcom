# Awards / hackathons scaffold — intentionally empty and not registered as an
# Astro collection, because an empty glob loader warns on every build (#654).
# When the first real entry lands, restore the collection schema in
# src/content.config.ts and wire AwardsSection into src/pages/resume.astro.
# Intended schema: name (required), issuer?, year?, order?, url?. See #394.
