# AI Context—nathanpayne.com

## Project Name
nathanpayne.com

## Project Type
Static personal portfolio, project case-study, and editorial content site built with Astro (static site generator). Outputs pre-rendered HTML/CSS/JS—no server runtime.

## Primary Languages
- Astro (`.astro` templates)
- TypeScript / JavaScript (ES modules)
- CSS3 (vanilla, custom properties)
- Markdown (blog and project content with YAML frontmatter)

## Key Entry Points

| File | Purpose |
|------|---------|
| `src/pages/index.astro` | Homepage—Mondrian grid layout, panel interactions |
| `src/pages/blog/index.astro` | Blog listing page |
| `src/pages/blog/[...slug].astro` | Dynamic blog post pages, including nested content paths |
| `src/pages/projects/[slug].astro` | Dynamic project detail pages (Content Collections) |
| `src/pages/projects/index.astro` | Project index grid—auto-populated from collection |
| `src/lib/blog-order.ts` | Shared blog category vocabulary and featured/category/date comparator for editorial surfaces |
| `src/lib/index-grid.ts` | Shared six-row index geometry and boundary-safe accent placement for the project and blog indexes |
| `src/content/projects/*.md` | Project source files—each generates a page and index entry |
| `src/pages/resume.astro` | Resume page (`/resume`)—rendered from the resume content collections + `src/components/resume/` section components; two-page print stylesheet |
| `src/content/{myself,experience,education,skills,certifications}/`, `src/content/resume/projects/` | Active resume content collections—one-file-per-entry source for `/resume` (`resumeProjects` is distinct from the `projects` collection) |
| `src/content/awards/` | Dormant resume scaffold, intentionally unregistered while empty; restore its collection and page wiring when the first real award entry lands |
| `src/components/MetadataStrip.astro` | Strip-only project metadata table; screenshot surface owned by `ProjectLayout` |
| `scripts/lint-prose.mjs` | Repository prose gate backed by Vale. It discovers repo-owned tracked Markdown/MDX across the repository and standalone YAML under `src/content`, restores Markdown frontmatter sequence coverage through an extraction pass, blocks spaced em dashes, and reports Chicago heading/table-header capitalization as warnings. JSON output is available for automation; the gate never writes source files. |
| `tests/mermaid-contrast.test.js` | Rendered-output accessibility check. It measures explicit node fills and label colors in built Mermaid SVG, including styles produced by `style` and `classDef`, without parsing Mermaid source. |
| `.vale-version` | Single source for the Vale version enforced by both local prose lint and the checksum-verifying CI installer. |
| `src/pages/rss.xml.ts` | RSS feed endpoint |
| `src/pages/404.astro` | Error page |
| `src/layouts/BaseLayout.astro` | Base wrapper—meta, SEO, structure |
| `src/layouts/BlogPost.astro` | Blog post layout (three-column Mondrian grid) |
| `src/layouts/ProjectLayout.astro` | Project page layout |
| `src/styles/global.css` | Shared styles—Mondrian homepage, project detail pages, blog, motion system, responsive breakpoint, accessibility |
| `src/content/blog/**/*.md` | Recursively discovered Markdown blog posts with required category and an optional featured flag |
| `src/content.config.ts` | Astro Content Collections schema (Zod) |
| `astro.config.mjs` | Astro configuration—site URL, integrations, markdown plugins |
| `firebase.json` | Firebase Hosting config—cache headers, security headers, deploy ignore rules |
| `AGENTS.md` | AI agent instructions and behavioral rules |

## Directories With Non-Obvious Purpose

| Directory | Purpose |
|-----------|---------|
| `src/pages/og-templates/` | Build-time-only OG image templates—Playwright screenshots them at 1200×630 (2× DPR), outputs to `dist/og/`, then removes them from the final build |
| `src/integrations/` | Custom Astro integrations. Two currently: `og-images.mjs` (build-time OG image generation and—riding on the same Playwright Chromium and static server—the `/resume/` PDF export) and `robots-sitemap.mjs` (auto-syncs the `Sitemap:` line in `dist/robots.txt` with the filename `@astrojs/sitemap` actually produced). Both must use `fileURLToPath(dir)` (not `dir.pathname`) when computing the dist directory in `astro:build:done` hooks—see #171, #173. `resume-pdf.mjs` also lives here: not an integration itself, but the build-time renderer `og-images.mjs` calls (#616). Mermaid rendering belongs to the Markdown Rehype pipeline and the shared sidebar renderer, not either integration. |
| `src/plugins/` | Custom Remark/Rehype processors (Mermaid diagram support, figure captions) |
| `src/components/resume/` | Per-section `/resume` components + `CompanyLogo.astro` (Logo.dev lookup with a styled-initials fallback cascade) |
| `src/layouts/` | Astro layout components (BaseLayout, BlogPost, ProjectLayout, OgCard) |
| `public/` | Static assets copied verbatim into `dist/` at build time (favicons, robots.txt, fonts) |
| `dist/` | Build output from `astro build`—deployed to Firebase Hosting (gitignored) |
| `tests/` | Vitest smoke tests for metadata, interaction, layout, and route coverage |
| `rules/` | Repository-level invariants enforced on all agents and contributors |
| `plans/` | Feature rollout and migration plans |
| `specs/` | Feature specifications and acceptance criteria |
| `scripts/ci/` | CI enforcement scripts (run by GitHub Actions and locally before commits) |
| `styles/` | Repository-owned Vale style rules (`CMOS.EmDash`, `CMOS.Titles`, and `CMOS.Capitalization`) |
| `docs/` | Extended documentation (agent processes, deployment guides) |
| `screenshots/og/` | Checked-in OG image screenshots for visual regression reference |
| `.github/screenshots/` | PR, issue, and generated audit screenshot evidence; narrative findings live in the central docs PRD, not in a top-level `bugs/` folder. |

## Directories Intentionally Absent

| Directory | Justification |
|-----------|--------------|
| `functions/` | No serverless or cloud functions. Static site served directly from Firebase Hosting. Add if backend functions are introduced. |

## External Dependencies

| Service | Purpose |
|---------|---------|
| Firebase Hosting | Static file hosting and CDN at nathanpayne.com |
| Cloudflare | DNS and CDN in front of Firebase Hosting. Managed `robots.txt` content (the `# BEGIN Cloudflare Managed content` block) is injected by Cloudflare and not in the repo—crawler `Disallow` rules there cannot be changed by editing `public/robots.txt`. Access policy and rationale: `plans/crawler-access-policy.md` (#615). |
| Google Analytics 4 | User engagement analytics (Measurement ID via `PUBLIC_GA_MEASUREMENT_ID`—public client identifier, env-injected, never committed; like the Logo.dev/PostHog tokens) |
| PostHog | Product analytics and session replay via `src/components/posthog.astro` (`PUBLIC_POSTHOG_PROJECT_TOKEN`—public ingest token, env-injected, never hardcoded). If unset, PostHog does not initialize. |
| Google Search Console | Sitemap submission and indexing reports. Sitemap URL is `https://nathanpayne.com/sitemap-index.xml` (declared in the build-managed `Sitemap:` line of `robots.txt`). |
| Google Fonts | Cormorant Garamond (serif) and Inter (sans-serif) typefaces |
| Logo.dev | Employer/school/issuer logos on `/resume` via a publishable client token (`PUBLIC_LOGODEV_KEY`)—read-only, ships in the static HTML like the GA Measurement ID, sourced from env (never committed). If unset, `/resume` renders styled initials. Defunct brands use self-hosted Commons SVGs in `public/images/logos/`. |
| Google Cloud SDK (`gcloud`) + 1Password-backed GCP ADC | Source credentials for keyless Firebase deploys and Google API access |

### Load-bearing npm dependencies that look removable

`@astrojs/markdown-remark` is a required devDependency that **no source file imports**. It backs the `markdown.remarkPlugins` / `markdown.rehypePlugins` hooks in `astro.config.mjs`, which is how the three custom plugins in `src/plugins/` run. A dependency-graph scan will report it as unused; removing it breaks `astro build`. `astro` and `@astrojs/markdown-remark` are both pinned to the exact same version (`7.2.2`), because Astro declares the latter as an exact-version peer; they move together or not at all. Separately, `typescript` is declared as the bounded range `>=6.0.3 <6.1.0`, not a caret range, because `typescript-eslint` peers `<6.1.0`. All three constraints, and the procedure for changing them, are specified in `rules/repo_rules.md` § Toolchain Constraints—that file is authoritative, this note only exists so the dependency is discoverable from the inventory.

### Build-time Mermaid dependency

`rehype-mermaid` is the maintained build-time renderer. Astro excludes Mermaid fences from Shiki, then renders them to inline SVG in the Markdown pipeline. Sidebar items use the same renderer and configuration through `src/lib/render-sidebar-mermaid.mjs`. The small site-owned adapters preserve required title/description metadata, figure semantics, and a clean failure fallback; they do not parse Mermaid grammar. No Mermaid runtime or CDN dependency ships to production browsers.

## Do Not Touch

| File/Path | Reason |
|-----------|--------|
| `.firebaserc` | Firebase project binding—changing this changes which project you deploy to |
| `firebase.json` | Cache-control, security headers, and deploy ignore rules—changes affect CDN behavior |
| `astro.config.mjs` | Core build configuration—changes affect routing, markdown processing, and integrations |
| `inspiration.jpg` | Tracked design reference file at the repo root; not deployed because Astro only emits build output from `src/` and `public/`. Do not modify or delete without explicit approval. |

## Standard Compliance Notes

- **`src/` present:** Astro source directory containing pages, layouts, content collections, plugins, integrations, and styles.
- **`dist/` present but gitignored:** Build output from `astro build`. Deployed to Firebase Hosting.
- **`public/` present:** Static assets (favicons, robots.txt, OG fonts) copied verbatim into `dist/` at build time.
- **`functions/` absent:** Intentional. No serverless functions. Documented above.
- **`docs/` present:** Extended documentation (agent processes, deployment guides). Documented above.
- **`scripts/ci/` present:** CI enforcement scripts run by GitHub Actions workflows.
- **`.cursor/rules/deploy.mdc`:** Valid Cursor configuration file providing deploy context to the Cursor agent. Mirrors `DEPLOYMENT.md`.
- **`.cursor/rules/project-conventions.mdc`:** Valid Cursor configuration file providing project conventions to the Cursor agent.

## Related Repositories

- **`mergepath`** (sibling directory)—Canonical template per the AI Agent Tooling Standard. Contains the `gcloud` wrapper, `op-firebase-deploy` / `op-firebase-setup` scripts, the 1Password-backed impersonation deployment guide, and the full standard definition. Reference when creating new projects or when deployment questions extend beyond this repo.
- **Deploy-auth invariant**—This repo intentionally uses the shared 1Password-backed `Private/GCP ADC` source credential for routine human `gcloud` and deploy work. Do not migrate it back to ADC-first or deploy-key auth without explicit human approval.
